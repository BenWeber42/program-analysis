# proven correct by brute-force
def f_inv(p, x, y):
    dlt = 333
    if ((p) > (100)):
        if ((x) == (300)):
            return (x) - (dlt), y
        else:
            b = 1
            return (x) - (dlt), (p) / ((x) - (dlt))
    else:
        if ((x) == (333)):
            return (x) - (333), y
        return (x) - (333), (p) / ((x) - (333))
